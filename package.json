{
  "name": "threejs-basictemplate",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "git add dist -f && git commit -m \"Building dist to github page\" && git subtree push --prefix dist origin build"
  },
  "devDependencies": {
    "vite": "^5.0.8"
  },
  "dependencies": {
    "three": "^0.160.1"
  }
}
